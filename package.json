{
  "name": "swagi-intech",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/bcryptjs": "^2.4.6",
    "vitest": "~3.2.0",
    "wrangler": "^4.113.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3"
  }
}
